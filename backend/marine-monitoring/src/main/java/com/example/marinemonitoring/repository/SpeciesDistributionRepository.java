package com.example.marinemonitoring.repository;

import com.example.marinemonitoring.model.SpeciesDistribution;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SpeciesDistributionRepository extends JpaRepository<SpeciesDistribution, Long> {
}
